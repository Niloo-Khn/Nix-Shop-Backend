import { randomUUID } from "node:crypto";
import { HttpApplication, InMemoryRepository, type Json, type Repository, requireFields } from "./core.js";
type Article={id:string,title:string,body:string}; type Ticket={id:string;email:string;subject:string;message:string;status:"open";createdAt:string};
const articles:Article[]=[{id:"shipping",title:"Shipping",body:"Standard shipping is free on orders of $60 or more."},{id:"returns",title:"Returns",body:"Contact us within 30 days if an item is not right for your pet."},{id:"payments",title:"Payments",body:"Payments are completed on our secure payment provider's hosted page."}];
export class HelpCenterService{constructor(private readonly articleRepository:Repository<Article>,private readonly ticketRepository:Repository<Ticket>){}articles(){return this.articleRepository.list();}async createTicket(input:unknown){const b=requireFields(input,["email","subject","message"]);return this.ticketRepository.save({id:randomUUID(),email:String(b.email).slice(0,254),subject:String(b.subject).slice(0,120),message:String(b.message).slice(0,4000),status:"open",createdAt:new Date().toISOString()});}}
export function createHelpApp():HttpApplication{const app=new HttpApplication("help-service");const service=new HelpCenterService(new InMemoryRepository(articles),new InMemoryRepository<Ticket>());app.route("GET","/health",async()=>({body:{service:"help",status:"ok"}}));app.route("GET","/articles",async()=>({body:await service.articles() as unknown as Json}));app.route("POST","/tickets",async c=>({status:201,body:await service.createTicket(await c.json()) as unknown as Json}));return app;}
